# Example environment file for /etc/pmmm/pmmm.env
# AWS-only: do not connect to Polymarket from the local machine.
# Copy to the instance, set secrets there, then restart pmmm-bot.service.

# Safety defaults (flip to false for live trading)
PMMB_TRADING__DRY_RUN=true
PMMB_TRADING__STARTUP_CANCEL_ALL=true

# Hard risk caps (recommended for canary)
PMMB_TRADING__MAX_USDC_EXPOSURE_PER_MARKET=10

# Infra / logging
PMMB_INFRA__LOG_LEVEL=info
PMMB_INFRA__AWS_REGION=eu-west-1

# Key management (derive CLOB API creds, proxy-safe wallet)
PMMB_KEYS__WALLET_MODE=PROXY_SAFE
PMMB_KEYS__PRIVATE_KEY_SOURCE=ENV
PMMB_KEYS__API_CREDS_SOURCE=DERIVE
PMMB_KEYS__PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Legacy env var aliases (accepted, but prefer PMMB_*):
# BOT_PRIVATE_KEY=0x...
# CLOB_ADDRESS=0x...                # proxy wallet (funder)
# CLOB_AUTO_DERIVE_ENABLED=true     # implies PMMB_KEYS__API_CREDS_SOURCE=DERIVE
# CLOB_API_KEY=...
# CLOB_API_SECRET=...
# CLOB_API_PASSPHRASE=...

# Optional: set explicit proxy wallet (funder) if you want to override/validate derivation
# PMMB_KEYS__FUNDER_ADDRESS=0xYOUR_PROXY_SAFE_ADDRESS

# Optional: data API user for reconciliation (recommended: proxy wallet address when PROXY_SAFE)
# PMMB_KEYS__DATA_API_USER=0xYOUR_PROXY_SAFE_ADDRESS

# If you prefer explicit CLOB API creds instead of derivation:
# PMMB_KEYS__API_CREDS_SOURCE=EXPLICIT
# PMMB_KEYS__CLOB_API_KEY=YOUR_UUID
# PMMB_KEYS__CLOB_API_SECRET=YOUR_SECRET
# PMMB_KEYS__CLOB_API_PASSPHRASE=YOUR_PASSPHRASE

# Polygon RPC (recommended private endpoint for merge loop performance)
# PMMB_ENDPOINTS__POLYGON_RPC_URL=https://polygon-mainnet.g.alchemy.com/v2/YOUR_KEY
